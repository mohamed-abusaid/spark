---
machine:
   java:
      version: openjdk8



test:
   override:
     - unset M2_HOME
     - ./dev/make-distribution.sh --name nerve-spark --tgz -Phadoop-2.6 -Dhadoop.version=2.6.0 -Pyarn -Phive -Phive-thriftserver -DskipTests -Pjava8-tests:
         timeout: 1800
